[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "dpc"
version = "0.1.0"
description = "Differential phase contrast imaging for scanning X-ray nanoprobe beamlines"
readme = "README.md"
license = { file = "LICENSE" }
authors = [
    { name = "Paul Quinn", email = "paul.quinn@stfc.ac.uk" },
]
keywords = ["DPC", "differential phase contrast", "X-ray", "phase retrieval", "synchrotron"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: BSD Software License",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: Physics",
]
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "scipy",
    "scikit-image",
    "h5py",
    "pyyaml",
    "pillow",
    "matplotlib",
    "nexus-io",
]

[project.optional-dependencies]
mcp = [
    "mcp",
]
dev = [
    "pytest",
    "pytest-cov",
]
cwl = [
    "cwltool",
]
toil = [
    "toil[cwl]",
]

[project.scripts]
dpc = "dpc.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.hatch.envs.default]
dependencies = ["pytest", "pytest-cov"]

[tool.hatch.envs.default.scripts]
test = "pytest {args}"
